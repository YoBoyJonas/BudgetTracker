name: budget_tracker
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  firebase_auth: ^4.3.0
  firebase_core: ^2.8.0
  flutter:
    sdk: flutter 
  # Firebase firestore database
  cloud_firestore: ^4.3.0
  hive_flutter: ^1.1.0
  date_format: ^2.0.7
  # Firebase storage for storing large files
  firebase_storage: ^11.1.1
  image_picker: ^0.8.7+4
  audioplayers: ^4.0.1
  provider:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/images/
  - assets/audio/